on:
  workflow_dispatch:
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - name: Print a message
      run: |
        deprecated=true
        if [[deprecated]]; then
          echo "::warning::Deprecated function found"
        fi
